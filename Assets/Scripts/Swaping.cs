using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class Swaping : MonoBehaviour//, IPointerEnterHandler
{
    /*
    [SerializeField] private int current;
    [SerializeField] private Inventory inventory;
    private int draggedObject = -1;
    public void OnPointerEnter(PointerEventData eventData)
    {
        draggedObject = FindObjectOfType<Dragable>().GetGameObject();
        //Debug.Log("setting " + current +" as to be, of the " + inventory);
        FindObjectOfType<Gui>().SetToBe(current, inventory);
    }*/
}
